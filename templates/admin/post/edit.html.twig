{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <input type="submit" value="Update" class="btn btn-success">
    {{ form_end(form) }}
{% endblock %}